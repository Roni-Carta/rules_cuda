filegroup(
    name = "nccl_shared",
    srcs = [
        "@nccl//:nccl_shared",
    ],
)

genrule(
    name = "perf_binaries",
    outs = ["hello.txt"],
    srcs = [
        "test.sh"
    ],
    cmd = "$(location test.sh) > $(OUTS)",
)

